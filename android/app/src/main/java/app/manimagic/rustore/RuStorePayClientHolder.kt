package app.manimagic.rustore

import android.content.Intent

/**
 * STUB: RuStore Pay SDK temporarily disabled (maven repo 404).
 * Will be re-enabled when RuStore maven is back online.
 */
object RuStorePayClientHolder {
    fun handleIntent(intent: Intent) {
        // No-op: RuStore Pay SDK temporarily unavailable
    }
}
